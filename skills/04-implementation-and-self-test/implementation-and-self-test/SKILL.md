---
name: implementation-and-self-test
description: "按已批准设计实现最小可审查变更并完成与风险匹配的自测；适用于开发阶段的功能或缺陷交付。"
---

# 实现与自测

在已批准的设计和任务范围内交付可维护、可审查的变更，并形成自测证据。此 SKILL 不重新设计需求、不替代独立代码评审，也不绕过项目质量门禁。

## 输入

- 需求包、设计评审结论、任务切片、验证策略和发布恢复预案。
- 项目编码规范、目标分支、依赖策略和本地检查要求。

## 工作方式

1. 确认当前基线、任务关联和受影响的验收标准；先识别会改变范围的设计缺口或阻塞依赖。
2. 将实现限制在一个可独立审查和回退的切片，遵守既有架构边界、接口契约、授权模型和错误处理约定；无关重构另行拆分。
3. 为变更添加或更新最接近风险来源的自动化测试，覆盖主流程、边界、失败和授权场景；按验证策略补充更高层检查所需的准备。
4. 本地运行相关检查并检查变更是否仍与需求、设计和契约一致；记录未执行或无法自动化的验证及其原因。
5. 更新受影响的代码内说明、契约、运行说明或用户文档，并准备清晰的变更摘要。

## 输出格式

```markdown
## 实现与自测记录
- 关联需求、设计与任务：
- 变更切片与范围：
- 已实现行为及对应验收标准：
- 新增/更新的测试及覆盖场景：
- 已运行检查与结果：
- 未执行验证、原因与后续计划：
- 更新的文档/契约：
- 已知限制、风险与发布注意事项：
```

## 停止条件

- 当变更可构建、可追溯、测试与风险匹配且本地必需检查通过时，交给质量验证和交接准备。
- 当实现暴露设计缺口时，暂停扩大变更，回到方案与计划；不得静默改变关键设计。
- 当验收含义不清时，回到需求澄清，不以开发者猜测替代业务决策。
